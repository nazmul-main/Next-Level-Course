"use strict";
{
    const rollnumber = [1, 2, 3];
    const mentors = ['M. X', 'M. Y', 'M. Z'];
    const boolArray = [true, false, true];
    const user = [
        {
            name: 'Adil',
            age: 53
        },
        {
            name: 'Root',
            age: 32
        }
    ];
    const add = (x, y) => x + y;
    add(20, 30);
    const manus = ['Mr. X', 'Mr.y'];
    const userWithId = [1234, { name: 'John', email: 'john@gmail.com' }];
}
